// Single source of truth for the "N°11 — The Archive Pack" limited drop,
// shown on the Home and New Arrivals pages. Change this one date to move
// the drop — both banners and their countdowns will stay in sync for
// every visitor, instead of each page rolling its own random countdown.
export const DROP_NAME = 'N°11 — THE ARCHIVE PACK'
export const DROP_UNITS = 300
export const DROP_TARGET = new Date('2026-07-15T18:00:00+05:30').getTime()
