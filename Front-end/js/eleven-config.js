/* ===== ELEVEN — API config =====
   One place to point the whole site at your backend.

   - Local development (Django running on your machine): leave this as the
     127.0.0.1 line.
   - Live site (deployed): comment out the localhost line and use the
     onrender.com line instead — or just edit the URL below if your
     backend URL changes later. */

window.ELEVEN_API_BASE = 'https://eleven-with-backend.onrender.com/api';

// window.ELEVEN_API_BASE = 'http://127.0.0.1:8000/api'; // ← local dev
